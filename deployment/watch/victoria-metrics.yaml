global:
  scrape_interval: 30s
  scrape_timeout: 10s

scrape_configs:
  - job_name: geoguessme-production
    metrics_path: /metrics
    bearer_token_file: /run/watch-metrics/production-metrics-token
    static_configs:
      - targets: [backend:8080]
